// Required imports for React enzyme tests
import React from 'react';
import { shallow } from 'enzyme';
import App from '../../../client/components/App';

// Check test suite works
test('Test suite is working', () => {
  expect(1).toBeLessThan(3)
})